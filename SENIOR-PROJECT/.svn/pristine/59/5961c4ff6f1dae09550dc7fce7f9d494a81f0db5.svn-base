using System;
using System.Collections.Generic;
using System.Linq;
using System.Web;

namespace PhungNoiProject.Services
{
    public class Calculate
    {

        public double calDiscount(double price) {

            double p = price * 0.7;
            return p;
        }
        public void calPrice(double dcPrice,int quantity) {
            double p = dcPrice * quantity;

             Console.Out.Write("" + p); 
        }
        public double calTotalPrice(double allPrice) {
            double p = 0;
            p = p + allPrice;
            return p;
        }
        

    }
}